/*

function handleSizeButtons(e) {
  if (e.target === "button-sixteen") {
    if (board.dataset.size === "16") {
      return;
    }
    else {
      board.dataset.size = "16";
    }
  }
  else if (e.target === "button-thirty-two") {
    if (board.dataset.size === "32") {
      return;
    }
    else {
      board.dataset.size = "32";
    }
  }
  else {
    if (board.dataset.size === "64") {
      return;
    }
    else {
      board.dataset.size = "64";
    }
  }
  
}

function playGame() {
  const boardContainer = document.querySelector("#board-container");
  const board = document.querySelector("#board");

}

*/